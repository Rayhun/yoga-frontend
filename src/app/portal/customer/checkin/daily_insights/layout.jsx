export const metadata = {
    title: 'Daily Insights',
    description: 'Daily Insights for Customer Check-in',
  };
  
  const Layout = ({ children }) => {
    return children;
  };
  
  export default Layout;
  