export const metadata = {
    title: 'Monthly Goal',
    description: 'Set and track your monthly goals',
  };
  
  const Layout = ({ children }) => {
    return children;
  };
  
  export default Layout;
  