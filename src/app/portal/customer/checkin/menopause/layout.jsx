export const metadata = {
    title: 'Menopause',
    description: 'Track your menopause symptoms and progress',
  };
  
  const Layout = ({ children }) => {
    return children;
  };
  
  export default Layout;
  