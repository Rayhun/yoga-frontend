export const metadata = {
    title: 'Cycle Insights',
    description: 'Cycle Insights for Customer Check-in',
  };
  
  const Layout = ({ children }) => {
    return children;
  };
  
  export default Layout;
