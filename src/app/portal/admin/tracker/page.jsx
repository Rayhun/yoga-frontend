import Tracker from '@/components/Tracker/TrackerList';

export const metadata = {
  title: 'Tracker',
};

const Page = () => {
  return (
    <div>
      <Tracker />
    </div>
  );
};

export default Page;
