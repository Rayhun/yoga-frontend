import TrackerDetails from '@/components/Tracker/TrackerDetails';
import Tracker from '@/components/Tracker/TrackerList';

export const metadata = {
  title: 'Tracker Details',
};

const Page = () => {
  return (
    <div>
      <TrackerDetails />
    </div>
  );
};

export default Page;
