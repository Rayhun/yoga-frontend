import ModulesList from '@/components/lms/module/ModulesList';

export const metadata = {
  title: 'Modules',
};

const Page = () => {
  return (
    <div>
      <ModulesList />
    </div>
  );
};

export default Page;
