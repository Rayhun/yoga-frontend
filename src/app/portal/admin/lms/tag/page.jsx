import TagList from '@/components/lms/tag/TagList';

export const metadata = {
  title: 'Tags',
};

const Page = () => {
  return (
    <div>
      <TagList />
    </div>
  );
};

export default Page;
