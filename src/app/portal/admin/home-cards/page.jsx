import HomeCards from '@/components/admin/HomeCards';

export const metadata = {
  title: 'Home Cards',
};

const Page = () => {
  return (
    <div>
      <HomeCards />
    </div>
  );
};

export default Page;
