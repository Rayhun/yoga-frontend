import LookupItemsList from '@/components/lms/lookup-item/LookupItemsList';

export const metadata = {
  title: 'Lookup Items',
};

const Page = () => {
  return (
    <div>
      <LookupItemsList />
    </div>
  );
};

export default Page;

