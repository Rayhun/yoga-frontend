import AdminNotifications from '@/components/admin/notifications';

export const metadata = {
  title: 'Notifications',
};

const Page = () => {
  return (
    <div>
      <AdminNotifications />
    </div>
  );
};

export default Page;
