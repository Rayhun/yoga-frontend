import GroupsList from '@/components/chat/group/GroupsList';

export const metadata = {
  title: 'Groups',
};

const Page = () => {
  return (
    <div>
      <GroupsList />
    </div>
  );
};

export default Page;
