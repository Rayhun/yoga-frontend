import StaffUsersList from '@/components/entities/staff/StaffUsersList';

export const metadata = {
  title: 'Staff Users',
};

const Page = () => {
  return (
    <div>
      <StaffUsersList />
    </div>
  );
};

export default Page;
