import PayoutList from "@/components/affiliates/Payout/PayoutList";

export const metadata = {
    title: 'Affiliates - Payout',
  };
  
  const page = () => {
    return <PayoutList />;
  };
  
  export default page;