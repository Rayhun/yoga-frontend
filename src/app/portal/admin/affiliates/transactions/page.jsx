import AdminTransactions from '@/components/affiliates/AdminTransactions';

export const metadata = {
  title: 'Affiliate Transactions',
};

const Page = () => {
  return <AdminTransactions />;
};

export default Page;

