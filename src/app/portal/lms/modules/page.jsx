import ModulesList from '@/components/lms/modules/ModulesList';

export const metadata = {
  title: 'Modules',
};

const Page = () => {
  return (
    <div>
      <ModulesList />
    </div>
  );
};

export default Page;
