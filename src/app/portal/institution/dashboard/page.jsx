import InstitutionDashboardContent from '@/components/certification/apply/InstitutionDashboardContent';

export const metadata = {
  title: 'Institution Dashboard',
};

const Page = () => {
  return <InstitutionDashboardContent />;
};

export default Page;
