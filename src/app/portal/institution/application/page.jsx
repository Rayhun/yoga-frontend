import ApplicationStatusContent from '@/components/certification/institution/ApplicationStatusContent';

export const metadata = {
  title: 'My Application | Institution',
};

const Page = () => {
  return <ApplicationStatusContent />;
};

export default Page;
